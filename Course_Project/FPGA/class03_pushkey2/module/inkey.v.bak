module inkey(
	in,
	out,
	clk
);

input in,clk;
output out;
reg [31:0]cnt;
reg flag;
localparam NoPUSH = 1'b0;
localparam PUSH = 1'b1;

always@(posedge clk) begin
if(in)
	flag <= NoPUSH;
else 
	flag <= PUSH;

case(flag)
NoPUSH:
PUSH:
endcase

end
endmodule